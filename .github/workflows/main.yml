name: "Vercel - udrics: lint"

on:
  push:
    branches:
      - main

permissions:
  contents: read
  statuses: write

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Notify Vercel
        uses: vercel/repository-dispatch/actions/status@v1
        with:
          name: "Vercel - udrics: lint"
